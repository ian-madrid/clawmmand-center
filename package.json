{
  "name": "clawmmand-center",
  "version": "1.0.0",
  "description": "Living productivity dashboard - One Organism",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && node -e \"require('fs').writeFileSync('public/version.txt', new Date().toISOString() + '-deploy-' + require('crypto').randomBytes(4).toString('hex'))\"",
    "start": "next start",
    "lint": "next lint",
    "update-version": "node -e \"require('fs').writeFileSync('public/version.txt', new Date().toISOString() + '-deploy-' + require('crypto').randomBytes(4).toString('hex'))\""
  },
  "dependencies": {
    "next": "14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.0",
    "typescript": "^5.3.0"
  }
}
